/// Filters for the list of labels.
enum LabelsFilter {
  /// Show all labels.
  all,

  /// Show only visible labels.
  visible,

  /// Show only hidden labels.
  hidden,

  /// Show only pinned labels.
  pinned,

  /// Show only locked labels.
  locked,
}
